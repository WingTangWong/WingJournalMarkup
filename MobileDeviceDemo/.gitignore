# The vision + OCR WASM engines are committed so the demo runs from the repo /
# GitHub Pages with no external calls. Ignore anything else dropped in vendor/.
vendor/*
!vendor/opencv.js
!vendor/tesseract/
!vendor/htr/

# Throwaway TLS certs for on-device testing
*.pem

# scratch / local test files
_*
